-- 부하 테스트 직전에 재고를 주입한다.
--
-- LocalDataSeeder 의 시드 재고 총합은 6,850개다(설계 문서 3.3 장). 500 TPS 로 1개씩 주문하면
-- 13.7초 만에 전 재고가 소진되고, 그 뒤로 측정하는 것은 "409 를 얼마나 빨리 뱉는가" 이지
-- 주문 처리량이 아니다. k6 DURATION(기본 60s) 동안 재고가 바닥나지 않도록 넉넉히 채워 넣는다.
--
-- 시더 자체는 고치지 않는다. 시더의 재고 분포(10~100, 11개마다 0)는 order-v1.http 로
-- 품절 409 를 확인하기 위한 것이라(2026-08-24 설계 문서 9장), 부하 테스트 사정으로 바꾸면
-- 그 목적이 깨진다. 그래서 시더가 아니라 이 스크립트로, 측정 직전에만 덮어쓴다.
UPDATE products SET stock = 100000000;
