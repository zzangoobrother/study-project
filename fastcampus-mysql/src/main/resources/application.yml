logging:
  level:
    org:
      springframework:
        jdbc:
          core: TRACE

spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306?rewriteBatchedStatements=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    password:
